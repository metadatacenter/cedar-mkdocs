# Phone Number Field

A phone number field collects a telephone number. In YAML its `type` is
`phone-number-field`. It takes no type-specific keys.

```yaml
- key: contact-phone
  type: phone-number-field
  name: Contact Phone
```
