# Phone Number Field

A phone number field collects a telephone number. It takes no type-specific keys; the field
type alone marks the value as a phone number, distinguishing it from ordinary text. In YAML
its `type` is `phone-number-field`.

```yaml
- key: contact-phone
  type: phone-number-field
  name: Contact Phone
```
