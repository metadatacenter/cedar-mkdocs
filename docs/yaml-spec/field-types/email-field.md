# Email Field

An email field collects an email address. In YAML its `type` is `email-field`. It takes no
type-specific keys.

```yaml
- key: contact-email
  type: email-field
  name: Contact Email
```
