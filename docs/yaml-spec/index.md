# CEDAR Model YAML Specification

This specification defines the YAML serialization of the CEDAR model.

The model is defined first, on its own terms and independently of any serialization. The
rest of the specification defines the YAML representation of this model, construct by construct.
