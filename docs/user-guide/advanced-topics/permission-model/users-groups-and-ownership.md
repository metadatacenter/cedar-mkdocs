# Users, Groups and Ownership

A **user** is an authenticated CEDAR account. A **group** is a named set of users. CEDAR also has a
built-in **Everyone** group containing every authenticated user.

Every workspace resource has an **owner**. Exactly one user can own a resource at a time; a group
cannot.
